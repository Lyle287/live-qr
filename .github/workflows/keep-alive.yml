name: Keep Alive
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping live-qr to prevent cold start
        run: curl -s -o /dev/null -w "Ping status: %{http_code}\n" https://live-qr-henna.vercel.app
